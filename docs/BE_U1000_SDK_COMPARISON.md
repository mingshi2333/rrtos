# BE-U1000 SDK Comparison

This note compares the SDK in `/home/mingshi/Project/PF/mcu-sdk-linux_2.1.1_20260119` with the current `rrtos` board support and records the supplements added in this repo.

## Current comparison

| SDK capability | SDK evidence | `rrtos` before | `rrtos` now |
| --- | --- | --- | --- |
| Focused GPIO LEDBlink app port | `Projects/HAL_examples/GPIO/GPIO_LEDBlink/main.c` | GPIO proof only lived inside the broader `be_u1000_demo` self-test lane | Added a dedicated `apps/be_u1000_gpio_ledblink/` app lane that reuses the existing BE-U1000 GPIO/board seams and documents a direct vendor-example-to-RTOS mapping |
| Focused GPIO input polling app port | `Projects/HAL_examples/GPIO/GPIO_InputPolling/main.c` | Button polling only appeared indirectly in the board self-test output | Added a dedicated `apps/be_u1000_gpio_inputpolling/` app lane that mirrors the vendor polling relationship with RTOS-native task flow and dedicated log validation |
| Focused UART printf app port | `Projects/HAL_examples/UART/UART_printf/main.c` | UART output existed only as boot/self-test infrastructure and shared console plumbing | Added a dedicated `apps/be_u1000_uart_printf/` app lane that proves vendor-style periodic UART printing through the existing console HAL and RTOS task flow |
| Focused CANFD polling app port | `Projects/HAL_examples/CANFD/CANFD_OneBoard_Communication_Polling/main.c` | CANFD proof only lived inside the broader board self-test lane | Added a dedicated `apps/be_u1000_canfd_polling/` app lane that exercises CANFD0 internal loopback and IRQ-backed progress markers through the existing CANFD HAL |
| Focused QSPI flash read app port | `Projects/HAL_examples/QSPI/QSPI_Flash/main.c` | QSPI proof only lived inside the broader board self-test lane and DFU docs | Added a dedicated `apps/be_u1000_qspi_flash/` app lane that performs non-destructive window-read and flash-identify checks through the existing flash HAL |
| Focused I2C polling groundwork app port | `Projects/HAL_examples/I2C/I2C_OneBoard_Communication_PollingAndIT/main.c` | I2C proof only lived inside the broader board self-test lane | Added a dedicated `apps/be_u1000_i2c_polling/` app lane that proves current header-I2C master init/probe flow through the existing I2C HAL, without claiming full one-board peer parity yet |
| Focused TIM timebase adaptation app | `Projects/HAL_examples/TIM/TIM_TimeBase/main.c` | The repo had timer sanity through CLINT and scheduler delay, but no dedicated focused timer example lane | Added a dedicated `apps/be_u1000_tim_timebase/` app lane that adapts the vendor timebase intent onto the current CLINT-backed RTOS timer seam without falsely claiming BE-U1000 peripheral TIM parity |
| Minimal ADC HAL + focused single-trigger app | `Projects/HAL_examples/ADC/ADC_RegularMode_Single_TriggerSW/main.c`, `Drivers/HAL/Inc/bmcu_adc.h`, `Drivers/HAL/Src/bmcu_adc.c` | The repo had only ADC base/IRQ constants and no HAL surface at all | Added `hal/include/hal_adc.h`, `hal/src/be_u1000/hal_adc_be_u1000.c`, and a focused `apps/be_u1000_adc_single/` lane that carries SDK-derived ADC register semantics into `rrtos` with timeout-safe single-trigger polling |
| Minimal WDT HAL + focused heartbeat app | `Projects/HAL_examples/WDT/WDT_Reset/main.c`, `Drivers/HAL/Inc/bmcu_wdt.h`, `Drivers/HAL/Src/bmcu_wdt.c` | The repo had only WDT base/IRQ constants and no HAL surface at all | Added `hal/include/hal_wdt.h`, `hal/src/be_u1000/hal_wdt_be_u1000.c`, and a focused `apps/be_u1000_wdt_heartbeat/` lane that carries SDK-derived watchdog config/reload semantics into `rrtos` without forcing a real reset |
| Minimal PWMG HAL + focused output compare app | `Projects/HAL_examples/PWMG/PWMG_OutputCompare/main.c`, `Drivers/HAL/Inc/bmcu_pwmg.h`, `Drivers/HAL/Src/bmcu_pwmg.c` | The repo had only PWMG base/IRQ constants and no HAL surface at all | Added `hal/include/hal_pwmg.h`, `hal/src/be_u1000/hal_pwmg_be_u1000.c`, and a focused `apps/be_u1000_pwmg_outputcompare/` lane that carries SDK-derived PWMG output-compare setup into `rrtos`, now with explicit ARR/CCR counter wrap, compare-match flag observation, and compare-interrupt-enable lifecycle helpers, but still without pretending to model pin-level waveform timing |
| Minimal DMA HAL + focused mem2mem groundwork app | `Projects/HAL_examples/DMA/DMA_MemoryToMemory_SRAM/main.c`, `Drivers/HAL/Inc/bmcu_dma.h`, `Drivers/HAL/Src/bmcu_dma.c` | The repo had only DMA base/IRQ constants and no HAL surface at all | Added `hal/include/hal_dma.h`, `hal/src/be_u1000/hal_dma_be_u1000.c`, and a focused `apps/be_u1000_dma_mem2mem/` lane that carries SDK-derived memory-to-memory channel programming into `rrtos` with timeout-safe transfer polling |
| Minimal I2S HAL + focused TX groundwork app | `Projects/HAL_examples/I2S/I2S_Slave_TX_DMAMode/main.c`, `Drivers/HAL/Inc/bmcu_i2s.h`, `Drivers/HAL/Src/bmcu_i2s.c` | The repo had only I2S base/IRQ constants and no HAL surface at all | Added `hal/include/hal_i2s.h`, `hal/src/be_u1000/hal_i2s_be_u1000.c`, and a focused `apps/be_u1000_i2s_tx/` lane that now carries SDK-derived I2S transmitter configuration plus bounded TX FIFO lifecycle observation into `rrtos` without claiming codec-level or DMA-streaming parity |
| Minimal PWMA HAL + focused timebase app | `Projects/HAL_examples/PWMA/PWMA_TimeBase/main.c`, `Drivers/HAL/Inc/bmcu_pwma.h`, `Drivers/HAL/Src/bmcu_pwma.c` | The repo had only PWMA base/IRQ constants and no HAL surface at all | Added `hal/include/hal_pwma.h`, `hal/src/be_u1000/hal_pwma_be_u1000.c`, and a focused `apps/be_u1000_pwma_timebase/` lane that carries SDK-derived PWMA timebase semantics into `rrtos`, now with clearer update-flag vs update-interrupt behavior plus ARR/RCR-driven overflow progression and live repetition-counter reprogramming while still stopping short of full advanced-channel parity |
| Minimal USB HAL + focused runtime groundwork app | `Projects/HAL_examples/USB/USB_CDC/main.c`, `Drivers/BASIS/Device/BMCU/Include/be_u1000.h` USB common/control registers | The repo had only USB base/IRQ constants plus DFU/boot docs and no runtime HAL surface at all | Added `hal/include/hal_usb.h`, `hal/src/be_u1000/hal_usb_be_u1000.c`, and a focused `apps/be_u1000_usb_runtime/` lane that carries minimal controller enable, soft-connect, `cfg=0x9e` / `power=0x60` controller-state observation, and indexed endpoint register-surface semantics into `rrtos`, now including EP0 `csr0=0x9` + `count0=8`, rotating `INTRTX` snapshots across `idx=0..3`, and `idx=2` `RXCSR`/`RXCOUNT` packet-ready observation, without pretending enumeration works yet |
| CLIC interrupt numbering and Core2 system-side TCM windows | `Drivers/BASIS/Device/BMCU/Include/be_u1000.h` | `board_config.h` used a partially shifted IRQ table and did not expose the system-side Core2 TCMA window | Audited `board_config.h` against the SDK, corrected the IRQ numbering, and added `BE_U1000_C2_TCMA_SYS_BASE` / `BE_U1000_C2_TCMB_SYS_BASE` |
| EVU-BA board pins for UART0 / user LED / user button / header I2C+SPI / QSPI1 | `BSP/EVU_BA_2_1/bsp.h`, `BSP/EVU_BA_2_1/bsp.c`, EVU-BA-2.3 tech description tail schematic pages | Board init only knew peripheral base addresses; it did not apply the BSP pinmux defaults, and the diagnostic GPIO pointed at the user button instead of the LED | `hal_board_init()` now applies EVU-BA UART0 and user LED/button pinmux defaults, also configures header `I2C0` (`PA4/PA5`), header `SPI1` (`PA8..PA11`), and board `QSPI1` (`PB0..PB5`); the diagnostic GPIO now targets the user LED on `PC0` |
| Systematic board pinmux groups | EVU-BA-2.3 tail schematic pages, SDK CANFD examples | Board pinmux knowledge lived in ad-hoc board init code only | Added `hal_board_apply_pinmux_group()` and a documented EVU-BA connector map in `docs/EVU_BA_PINMAP.md` |
| Board bring-up self-test demo | EVU-BA connector map + current HAL driver surfaces | Demo only checked generic GPIO/SPI/I2C init using a single diag tuple | `apps/be_u1000_demo/main.c` now runs a richer EVU-BA self-test covering LED/button GPIO, header `I2C0`, header `SPI1`, and readiness reporting for `QSPI1`, `CANFD0`, and `CANFD1` |
| Renode self-test coverage for SPI/QSPI | `scripts/simulation/be_u1000.repl`, `scripts/be_u1000/run_smp_runtime.py` | Simulation had no `SPI1` controller behavior and no `QSPI1` read validation | Added a minimal `SPI1` Python stub plus preloaded `QSPI1` signature words so simulation can validate polled SPI transfer and non-destructive QSPI window reads |
| Core1/Core2 explicit start/reset control | `Drivers/HAL/Inc/bmcu_cru.h`, `Drivers/BASIS/Device/BMCU/Include/be_u1000.h` | Startup assembly existed, but there was no reusable board HAL API for core control | Added `hal_corectrl_*` board HAL wrappers |
| Core2 helper workflow | `Projects/HAL_examples/GPIO/GPIO_LEDBlink_C2/README.md` | Core2 was documented as reserved, but there was no helper abstraction for image copy/start | Added `hal_corectrl_core2_copy_image()` and explicit Core2 control helpers |
| Mailbox system-side helpers | `Drivers/HAL/Inc/bmcu_mb.h` | Mailbox IRQ/base constants existed, but no board HAL wrapper was exposed | Added `hal_mailbox_*` helpers for system-side DATA0/DATA1 access |
| Core0/Core1 SMP affinity demo | `FreeRTOS ... RISC-V_Baikal_SMPDemo_GCC/main.c` | No explicit affinity-pinned board demo | Experimental demo now validates `control -> Core0`, `worker -> Core1` |

## Added board HAL surfaces

- `hal/include/hal_corectrl.h`
- `hal/src/be_u1000/hal_corectrl_be_u1000.c`
- `hal/include/hal_mailbox.h`
- `hal/src/be_u1000/hal_mailbox_be_u1000.c`

## Added board config audit points

- `boards/be_u1000/board_config.h` now follows the SDK CLIC numbering instead of the earlier approximate table.
- `scripts/be_u1000/validate_interrupt_consistency.py` can now validate both the Renode model and the vendor SDK header in one pass.
- `pixi run -e be-u1000 validate-irq-map` now checks the repo board config against the SDK header as well as the Renode platform file.
- `docs/EVU_BA_PINMAP.md` records the connector/header recovery from the EVU-BA-2.3 tail schematic pages and notes which tables remain partial.

## Why these supplements matter

- They move SDK-only board knowledge into reusable `rrtos` HAL layers instead of leaving it trapped in startup code or ad-hoc examples.
- They prepare the next step after `Core0/Core1` SMP: a `Core2` helper image started and fed by `Core0` through mailbox-style communication.

## Remaining gaps against the SDK

- The first focused app ports now exist for `GPIO_LEDBlink`, `GPIO_InputPolling`, `UART_printf`, `CANFD_OneBoard_Communication_Polling`, `QSPI_Flash`, I2C master-probe groundwork, a CLINT-backed `TIM_TimeBase` adaptation, a minimal SDK-derived ADC single-trigger lane, a minimal SDK-derived watchdog heartbeat lane, a minimal SDK-derived PWMG output-compare lane with compare-match plus compare-interrupt-enable observation, a minimal SDK-derived DMA mem2mem groundwork lane, a minimal SDK-derived I2S TX groundwork lane with bounded FIFO fill/drain, overrun, and flush observation, a minimal SDK-derived PWMA timebase lane with clearer update lifecycle observation and live repetition-counter reprogramming, and a minimal USB runtime groundwork lane with indexed endpoint `INTRTX`/CSR/RXCSR/count snapshots, but `GPIO_InputIT`, `Running_Lights`, UART interrupt/DMA variants, true external-bus CANFD proof, full one-board I2C parity, destructive flash flows, full ADC parity, real BE-U1000 peripheral TIM parity, real reset-grade watchdog parity, true PWM waveform proof, silicon-faithful DMA transfers, full I2S audio path parity, advanced PWMA channel/IRQ parity, actual USB enumeration/data-path parity, and Core2 GPIO variants still need follow-on work.
- `rrtos` still does not run a true separate `Core2` helper image end-to-end.
- Current `Renode` modeling is still approximate and not a full substitute for silicon validation.
- Connector-level `XP8` / `XS3` / `XP9` ordering is still only partially recovered from text extraction, but UART0, user LED/button, header `I2C0`, header `SPI1`, `QSPI1`, and optional CANFD pinmux groups are now modeled in board support.
